# Novolis.Audio.Host.Abstractions

Game host audio output contract.

## Install

```bash
dotnet add package Novolis.Audio.Host.Abstractions
```

## Quick start

Reference this package from host adapters (for example `Novolis.Audio.Host.NAudio`) that implement game output behind `IAudioHostOutput`.
